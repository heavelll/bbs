<!DOCTYPE html>
html
  head
    meta(charset="utf-8")
    title 用户注册
  body
    include head.pug
    div(class="container" style="max-width:375px")
      form(class="form-signin" action="/register" method="post" enctype="multipart/form-data")
        h2(class="form-signin-heading") 用户注册
        input(name="name" class="form-control" placeholder="用户名" required autofocus)
        br
        input(name="password" class="form-control" placeholder="密码" required)
        br
        input(name="email" type="email" class="form-control" placeholder="邮箱" required)
        br
        span 请上传头像:
        input(name="avatar" type="file")
        br
        button(class="btn btn-lg btn-primary btn-block" type="submit") 注册